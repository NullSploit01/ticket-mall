apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      containers:
        - name: auth
          image: nullsploit/auth

---
apiVersion: v1
kind: Service
metadata:
  name: auth-srv
name:
  selector:
    app: auth
  ports:
    - name: auth
      protocol: tcp
      port: 3000
      targetPort: 3000
